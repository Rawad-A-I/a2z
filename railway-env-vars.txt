# Railway Environment Variables for Django eCommerce
# Copy these to Railway dashboard → Variables

# Django Core Settings
SECRET_KEY=!(ixk#o$mmbrf@f60o^r=k(m#3yqak*z_vvlet@xklj@f3)8ie
DEBUG=False
ALLOWED_HOSTS=*.railway.app
CSRF_TRUSTED_ORIGINS=https://*.railway.app

# Database (Auto-provided by Railway PostgreSQL)
# DATABASE_URL will be automatically set by Railway

# Email Configuration (Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=rawad.s.abouibrahim@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=rawad.s.abouibrahim@gmail.com

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles

# Media Files
MEDIA_ROOT=/app/media
MEDIA_URL=/media/

# Security Settings
SECURE_SSL_REDIRECT=True
SECURE_PROXY_SSL_HEADER=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Performance & Caching
CACHE_TTL=300

# Optional: Redis for caching (if you add Redis service)
# REDIS_URL will be automatically set if Redis is added

# Optional: Elasticsearch for search (if you add Elasticsearch service)
# ELASTICSEARCH_HOSTS will be automatically set if Elasticsearch is added
